-- ============================================================
-- No-op (ยกเลิกรัน backfill อัตโนมัติ)
--
-- เดิมสคริปต์นี้จะยกเลิก order_delivery_trip_allocations ค้าง + เรียก RPC รีแคล์ยอด
-- ถูกปิดการรันแล้ว — การซ่อมข้อมูลทำได้จากแอปหลังแพตช์ tripCrudService / allocationService
--    หรือรัน SQL เองใน Dashboard ตามความจำเป็น
--
-- หมายเหตุ: ถ้าเคย apply เวอร์ชันเก่าของไฟล์นี้บน DB ไปแล้ว แถวในประวัติ supabase_migrations ยังคงอยู่
--         จะไม่รันซ้ำบน environment นั้นอยู่แล้ว ไฟล์นี้มีผลกับ environment ที่ยังไม่เคยรันเท่านั้น
-- ============================================================

SELECT 1;
